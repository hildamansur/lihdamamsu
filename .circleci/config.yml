version: 2
jobs:
  abas:
    docker:
      - image: cimg/python:3.8

    steps:
    - checkout
    - run: ls -a
    - run: cd .. && ls -a
    - run: cd .. && .. && ls -a
    - run: cd .. && .. && .. && ls -a
    - run: cd .. && .. && .. && .. && ls -a






workflows:
  version: 2
  abas:
    jobs:
    - abas
